# Worshop sobre pruebas unitarias con Jest

## 1ra sesión:

### Instalar Jest

`npm i jest @types/jest -D`

### Configurar scripts, en `package.json`

```
"scripts": {
  "test": "jest --watchAll"
}
```
